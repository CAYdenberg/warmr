
describe('pointsFromSlope', () => {
  it('should calculate a series of linear data points from a single point and a slope')
})

describe('lastValue', () => {
  it('should find the last value in an array')
})

describe('findSeriesIndexByLastValue', () => {
  it('should find the index of a series by its last value')
})

describe('integrateData', () => {
  it('should add up a series of datapoints')
})

describe('integrateLinear', () => {
  it('should calculate the area under a line from a pair of x/y coordinates')
})
